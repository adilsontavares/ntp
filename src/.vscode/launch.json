{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "type": "php",
            "request": "launch",
            "program": "${workspaceRoot}/ntpdate.php",
            "cwd": "${fileDirname}",
            "args": [
                "time.apple.com"
            ],
            "port": 9000
        }
    ]
}